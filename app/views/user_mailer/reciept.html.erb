<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Thank you for your order!</h1>
    <p>
      You have successfully placed an order on Jungle!
    </p>
    <% @order.line_items.each do |i|  %>
     <h2> <%= i.quantity %>  <%= i.product.name %> - <%= i.total_price_cents %></h2>
    <% end%>
    <h4>Total Items: <%= @order.line_items.count %> </h4>
    <h4>Total Sale: <%= @order.total_cents %></h4>
    <p>
      To view your order details, just follow this link: <%= @url %>.
    </p>
    <p>Thanks for your purchase and have a great day!</p>
  </body>
</html>
